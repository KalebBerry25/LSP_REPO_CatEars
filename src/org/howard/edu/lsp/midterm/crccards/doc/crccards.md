TaskManager collaborates with Task because its responsibilities include storing tasks, adding new tasks, finding a task by ID, and returning tasks by status, which means it  will have to interact with Task objects. Task, however, is only responsible for storing its own information, updating its own status, and providing its own details, so it does not need to know anything about TaskManager. This is a one-way collaboration where TaskManager depends on Task, but Task remains independent.